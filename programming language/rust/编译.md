# 编译

记录日常编译中遇到的问题及解决方法

## 使用静态连接库

使用动态连接库时，将二进制分发到不同的平台经常会遇到xx.so未找到的问题。因此需要使用静态连接连接动态库对应的静态库。在rust中可以在build.rs中添加"cargo:rust-clink-lib=static=xx静态库"这样的语句。

~~~rust
fn main() {
    println!("cargo:rustc-link-lib=static=pcap");
    println!("cargo:rustc-link-lib=static=ssl");
 }
~~~

## 交叉编译

