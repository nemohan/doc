# 内存分配失败

最近将一个AMD64平台下的c语言项目迁移到ARM平台时，遇到了SIGSEGV错误。通过gdb调试发现是项目中对malloc进行封装的内存分配函数返回的内存地址出现了变化，变成了无效的内存地址(malloc返回的内存地址是有效的)。而且地址(64位)最高的32位发生了变化，最低32位没发生变化。通过反汇编发现封装的函数返回的地址赋值给本地变量的指令是"STRW 内存地址  w0"，正常应该是"STRD 内存地址 x0"。后来发现是迁移的时候，内存分配函数的原型未定义在头文件中，导致编译器默认该函数的返回值是int类型(4字节)。所以就产生了上述bug

